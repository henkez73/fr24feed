# fr24feed

![workflow](https://github.com/henkez73/fr24feed/actions/workflows/main.yml/badge.svg)

The `fr24feed` container image can be pulled from docker, e.g. `docker pull henkez/fr24feed:latest`.

The `dump1090` container image can be pulled from docker, e.g. `docker pull henkez/dump1090:latest`.
